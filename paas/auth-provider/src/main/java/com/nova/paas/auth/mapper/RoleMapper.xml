<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nova.paas.auth.mapper.RoleMapper">
    <resultMap id="role" type="com.nova.paas.auth.entity.Role">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="roleCode" column="role_code"/>
        <result property="roleName" column="role_name"/>
        <result property="roleType" column="role_type"/>
        <result property="description" column="description"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="modifiedBy" column="modified_by"/>
        <result property="modifiedAt" column="modified_at"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <insert id="insert" parameterType="com.nova.paas.auth.entity.Role">
        insert into au_role (id, tenant_id,role_code,role_name,role_type,description,
        created_by,created_at)
        values (#{id}, #{tenantId},#{roleCode},#{roleName},#{roleType},
        #{description},#{createdBy},#{createdAt})
    </insert>

    <update id="batchDelete">
        update au_role set del_flag=1,modified_by=#{modifiedBy},modified_at=#{modifiedAt} WHERE tenant_id=#{tenantId}
        <if test="roleIds != null and roleIds.size()>0">
            AND id IN
            <foreach item="item" collection="roleIds" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        and del_flag=0
    </update>

    <update id="update" parameterType="com.nova.paas.auth.entity.Role">
        update au_role
        <set>
            <if test="roleCode != null">
                role_code = #{roleCode},
            </if>
            <if test="roleName != null">
                role_name = #{roleName},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="modifiedBy != null">
                modified_by = #{modifiedBy},
            </if>
            <if test="modifiedAt != null">
                modified_at = #{modifiedAt},
            </if>
        </set>
        where id = #{id} and tenant_id=#{tenantId}
    </update>

    <select id="queryRoleListByPage" resultMap="role">
        select * from au_role WHERE tenant_id=#{tenantId}
        <if test=" ids!= null and ids.size()>0">
            AND id IN
            <foreach item="item" collection="ids" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="roleName != null and roleName!=''">
            AND role_name=#{roleName}
        </if>
        <if test="roleCode != null and roleCode!=''">
            AND role_code=#{roleCode}
        </if>
        <if test="roleType != null ">
            AND role_type=#{roleType}
        </if>
        and del_flag=0
        <if test="page!=null ">
            limit #{page.start},#{page.pageSize}
        </if>
    </select>

</mapper>
